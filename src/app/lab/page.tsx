import { LabView } from "@/views/LabView";
export const metadata = {
  title: "AI Lab",
  description: "How GenAI is used across the work: the pipeline, where models fail, and the production systems behind the library.",
};
export default function Page() {
  return <LabView />;
}
